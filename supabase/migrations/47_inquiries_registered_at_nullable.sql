-- ============================================================================
-- 問合せ(inquiries)の登録日時を NULL 許容に変更 (2026-06)
--
-- 背景: 取込時、CSVの登録日時が空の行を「取込日時(現在)」でフォールバックしていたため、
--       実際には日時不明のレコードが取込日になってしまう。空は NULL(空欄)で保持する方針に変更。
-- ============================================================================

ALTER TABLE public.inquiries ALTER COLUMN registered_at DROP NOT NULL;
