-- =============================================================================
-- import_sources に「2つ目のファイル」枠を追加 (CLAUDE.md §5.10c 拡張)
-- =============================================================================
-- 目的:
--   問合せ(inquiries)は2つのフォーム由来CSV(KAWARA版 / 機密保持・CP)を
--   1テーブルに統合する。定期取込でも両ファイルを1オブジェクトに紐づけられるよう、
--   2つ目の Drive ファイルID列を追加する(他オブジェクトでは未使用でも害なし)。
--
-- 影響範囲: import_sources に nullable 列を1つ追加するのみ。冪等。
-- =============================================================================

ALTER TABLE public.import_sources
  ADD COLUMN IF NOT EXISTS drive_file_id_2 text;
