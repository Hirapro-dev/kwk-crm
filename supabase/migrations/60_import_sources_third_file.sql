-- =============================================================================
-- import_sources に「3つ目のファイル」枠を追加 (CLAUDE.md §5.10c 拡張)
-- =============================================================================
-- 目的:
--   問合せ(inquiries)は複数フォーム由来CSV(KAWARA版 / 機密保持・CP / 総合問合せ 等)を
--   1テーブルに統合する。既存の2枠(drive_file_id / drive_file_id_2)では足りないため、
--   3つ目の Drive ファイルID列を追加する(他オブジェクトでは未使用でも害なし)。
--
-- 影響範囲: import_sources に nullable 列を1つ追加するのみ。冪等。
-- =============================================================================

ALTER TABLE public.import_sources
  ADD COLUMN IF NOT EXISTS drive_file_id_3 text;
