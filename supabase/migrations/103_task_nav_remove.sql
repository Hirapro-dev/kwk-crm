-- ============================================================================
-- migration 103: タスク管理をメーラーと同じ独立画面にし、CRM のメニューバーから「タスク」を外す (2026-09-18) / CLAUDE.md §8.1
--   migration 102 で入れた nav_items の 'task' を削除する(ヘッダーのタスクアイコンとアプリランチャーから別タブで開く)
-- ============================================================================
DELETE FROM public.nav_items WHERE id = 'task';
